# ClawCartel

**This repo uses separate branches for each part of the project.**

## Branches

| Branch       | Description           |
|-------------|------------------------|
| **`frontend`** | Web / UI application  |
| **`backend`**  | API / server code     |

Switch to `frontend` or `backend` (branch dropdown on GitHub) to view the code.
